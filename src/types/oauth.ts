export type TOauthProviderId = 'discord' | 'github' | 'google';

export type TOauthAuthorizationUrls = {
  discordAuthorizationUrl: string;
  githubAuthorizationUrl: string;
  googleAuthorizationUrl: string;
};
