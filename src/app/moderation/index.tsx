import { ScreenStub } from '@/components/screen-stub';

export default function ModerationScreen() {
  return (
    <ScreenStub
      title="Moderation"
      description="Pending posts, reports, and owner amend APIs already exist under /api/moderation/*."
      webPath="/moderation"
    />
  );
}
