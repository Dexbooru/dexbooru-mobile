import { ScreenStub } from '@/components/screen-stub';

export default function SimilaritySearchScreen() {
  return (
    <ScreenStub
      title="Similarity search"
      description="Web uses a form action only. Mobile needs a JSON similarity-search API."
      webPath="/similarity-search"
    />
  );
}
